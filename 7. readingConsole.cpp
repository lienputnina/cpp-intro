#include <iostream>
using namespace std;

int main() {
  // Taking a value and putting it OUT to the console
  // cout << "Enter a number value";
  // int inputValue;
  // Reading a value and putting it IN a variable
  // cin >> inputValue;
  // cout << inputValue;

  // Taking a value and putting it OUT to the console
  cout << "Enter values for x and y";
  double x;
  double y;

  cin >> x >> y;
  cout << x + y;

  return 0;
}